{
  "dockerhub": {
    "args": [
      "run",
      "-i",
      "--rm",
      "-e",
      "HUB_PAT_TOKEN",
      "mcp/dockerhub",
      "--transport=stdio",
      "--username=${env:HUB_USERNAME}"
    ],
    "command": "docker",
    "env": {
      "HUB_PAT_TOKEN": "${env:HUB_PAT_TOKEN}"
    }
  }
}
