api_version: v1

servers:
  cloudflare-bindings:
    description: Official Cloudflare Workers Bindings MCP server for storage, AI, and compute primitives.
    type: remote
    remote:
      protocol: streamable_http
      url: "https://bindings.mcp.cloudflare.com/mcp"
    targets:
      - claude
      - codex-package
      - gemini
      - opencode
      - cursor
