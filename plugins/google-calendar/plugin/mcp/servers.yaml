api_version: v1

servers:
  google-calendar:
    description: Anthropic-hosted Google Calendar connector for Claude over remote streamable HTTP.
    type: remote
    remote:
      protocol: streamable_http
      url: "https://gcal.mcp.claude.com/mcp"
    targets:
      - claude
