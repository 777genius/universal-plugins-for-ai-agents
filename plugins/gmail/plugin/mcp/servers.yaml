api_version: v1

servers:
  gmail:
    description: Anthropic-hosted Gmail connector for Claude over remote streamable HTTP.
    type: remote
    remote:
      protocol: streamable_http
      url: "https://gmail.mcp.claude.com/mcp"
    targets:
      - claude
